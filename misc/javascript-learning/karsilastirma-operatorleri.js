// OPERATÖRLER

let veri;
const a = 20;
const b = 10;
const c = 5;
let d = 3;

// karşılaştırma operatörleri

veri = a == b;
veri = b == c;
veri = 5 === 5; // veri türü ve sayılara 3 eşittir ile bakılır
veri = a != b; // ! ve = ile yaparsın ve eşitsizliği belirtmek için kullanırsın
veri = a !== b; // çift eşittir ile de yapabilirsin
veri = a > b; // büyük-küçük farkı için kullanırsın
veri = b < c; // küçük-büyük farkı için kullanırsın
veri = 5 >= 5; // > ve = ile yaparsın,
veri = 5 <= 5; // < ve = ile yaparsın, 

console.log(veri);
console.log(typeof veri);